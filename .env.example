HOSTING_ROOT=/media/ssdmount/websites-v2
INSTALLATION_ROLE=standalone
SERVER_ID=hosting-server
HOSTING_MACHINE_STATE_DIR=/etc/hosting-control
# Standby installs use /etc/hosting-control/ui-data; primary/standalone use app-data/ui-manager.
UI_DATA_DIR=/media/ssdmount/websites-v2/app-data/ui-manager
BACKUPS_DIR=/media/ssdmount/websites-v2/backups
EXPORTS_DIR=/media/ssdmount/websites-v2/backups/exports
# Optional dedicated Cloudflare Tunnel. Provision its mode-0400 token file
# administratively for the container's fixed uid 65532; never store it in Git
# or in the environment file.
HOSTING_TUNNEL_ENABLED=false
HOSTING_TUNNEL_TOKEN_FILE=/etc/hosting-control/cloudflared-hosting.token

# Project-owned Syncthing, separate from any host Syncthing service.
SYNC_CONFIG_DIR=/etc/hosting-control/syncthing
SYNC_LISTEN_IP=0.0.0.0
SYNC_PORT=22001
SYNC_GUI_LISTEN_IP=127.0.0.1
SYNC_GUI_PORT=8834
SYNC_MEMORY_LIMIT=2g
SYNC_CPU_LIMIT=1.50
# Expected Syncthing device ID for the other hosting server. When set, panel
# replication health ignores unrelated connected Syncthing devices.
HOSTING_SYNC_PEER_DEVICE_ID=

UI_ADMIN_EMAIL=panel-admin@example.com
UI_ADMIN_PASSWORD=replace-with-a-unique-long-panel-password
# Optional stable encryption key. If empty, the panel generates one in its data volume.
UI_SETTINGS_KEY=
HOSTING_AGENT_TOKEN=replace-with-a-long-random-token

# Separate billing administrator and narrow panel-to-billing API credential.
BILLING_ADMIN_EMAIL=billing-admin@example.com
BILLING_ADMIN_PASSWORD=replace-with-a-unique-long-billing-password
BILLING_API_TOKEN=replace-with-a-long-random-billing-api-token
BILLING_BACKUP_RETENTION=14
BILLING_SETTINGS_KEY=

# Optional off-site bootstrap secrets. Prefer entering them in the authenticated
# Backups panel, where they are encrypted with UI_SETTINGS_KEY.
OFFSITE_ACCESS_KEY_ID=
OFFSITE_SECRET_ACCESS_KEY=
OFFSITE_REPOSITORY_PASSWORD=
# Maximum completed/failed job records retained by the panel.
JOB_HISTORY_LIMIT=250
PROVISION_CREDENTIAL_TTL_HOURS=24
# Maximum complete portable bundle upload size (default 16 GiB).
TRANSFER_BUNDLE_UPLOAD_LIMIT_BYTES=17179869184

# Nginx Proxy Manager administrator. On an empty NPM database these values
# create the first administrator; the panel then uses the same API account.
NPM_API_URL=http://hosting-npm:81/api
NPM_IDENTITY=npm-admin@example.com
NPM_SECRET=replace-with-a-unique-long-npm-password
ACME_EMAIL=admin@example.com

# File Browser first administrator. Applied only when filebrowser.db is absent.
FILEBROWSER_ADMIN_USERNAME=file-admin
FILEBROWSER_ADMIN_PASSWORD=replace-with-a-unique-long-filebrowser-password

# API token permissions: Zone:Read and DNS:Edit for the managed zones.
CLOUDFLARE_API_TOKEN=replace-with-cloudflare-token
# Use a separate least-privilege token with zone read and Rulesets/WAF write.
# Cloudflare permission labels vary between user-owned and account-owned tokens.
CLOUDFLARE_SECURITY_API_TOKEN=replace-with-cloudflare-security-token
CLOUDFLARE_ACCOUNT_ID=replace-with-32-character-account-id
IPINFO_TOKEN=

MYSQL_SITE_PREFIX=yogali00_

# Persistent database credentials. Generate unique values before first start.
MYSQL_ROOT_PASSWORD=replace-with-a-long-random-password
NPM_DB_USER=npm_db
NPM_DB_PASSWORD=replace-with-a-long-random-password
NPM_DB_NAME=npm_db

# Machine-local resource overrides. Keep these in each server's private .env;
# a standby must not inherit the primary host's capacity assumptions.
STANDBY_PROFILE_NAME=
HOSTING_PEER_HEALTH_URL=
HOSTING_PEER_SERVER_ID=
HOSTING_PEER_API_TOKEN=
HA_PEER_SSH_HOST=
HA_PEER_ROOT=/media/ssdmount/websites-v2
HA_PEER_SYNC_DEVICE_ID=
HA_LOCAL_SYNC_DEVICE_ID=
HA_PEER_SYNC_ADDRESS=
HA_LOCAL_SYNC_ADDRESS=
MYSQL_SERVER_ID=1
MYSQL_INNODB_BUFFER_POOL_SIZE=2G
MYSQL_INNODB_REDO_LOG_CAPACITY=1G
MYSQL_MAX_CONNECTIONS=150
REDIS_MAXMEMORY=1024mb
# Optional absolute machine-local PHP configuration file. Leave empty to use
# HOSTING_ROOT/app-data/configs/php/global.ini.
PHP_GLOBAL_INI_PATH=
